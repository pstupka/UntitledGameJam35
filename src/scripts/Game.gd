extends Node2D

signal game_start
signal game_over(score, type)
signal game_exit

var bounds_x = 350
var bounds_y = 300

var score = 0





# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
